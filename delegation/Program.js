export default class Program {
    constructor() {

    }

    printMessage(message) {
        console.log(`The message to print is : ${message}`);
        return `The message to print is : ${message}`;
    }
}